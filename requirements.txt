# ═══════════════════════════════════════════════════════════════
# requirements.txt — ReSan Private Bank
# Install all dependencies with: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

fastapi==0.111.0          # Web framework for building the REST API
uvicorn[standard]==0.29.0 # ASGI server to run FastAPI
python-multipart==0.0.9   # Required for file uploads (UploadFile/Form support)
pydantic==2.7.1           # Data validation for request/response models